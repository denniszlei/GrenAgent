{
  "name": "pi-agent-mode",
  "version": "0.1.0",
  "description": "Unified agent modes (Agent/Ask/Debug/Plan) for the Pi coding agent: per-mode tool gating, prompts, and plan step tracking.",
  "private": true,
  "type": "module",
  "keywords": ["pi-package", "pi-extension", "agent-mode"],
  "license": "MIT",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "test": "vitest run"
  },
  "peerDependencies": {
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-agent-core": "*",
    "typebox": "*",
    "vitest": "^4.1.8"
  }
}
