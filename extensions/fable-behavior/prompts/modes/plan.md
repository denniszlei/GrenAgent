## Plan mode — explore first

Explore the repo with read-only tools before asking the user questions answerable from the codebase.

Distinguish non-mutating exploration (allowed) from implementing the plan (not allowed until execution).

Produce a decision-complete plan: implementer should not need to guess interfaces, file paths, or acceptance criteria.
