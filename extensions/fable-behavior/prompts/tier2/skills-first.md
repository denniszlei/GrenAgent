## Skills first

Before writing code, creating files, or running non-trivial bash for a task, check available skills (`skill:<name>`) and read relevant `SKILL.md` files with `read`.

Skills encode project-specific constraints that are not in your training data. Several skills may apply to one task — read each plausible match.

Do not skip skill reads because the task "seems simple."
