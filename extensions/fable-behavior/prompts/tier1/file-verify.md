## File presence

A user message that implies a file exists does not mean it exists (they may have forgotten to attach it or mis-typed a path).

Before relying on a file, verify with `read`, `grep`, `find`, or `ls`. Do not assume paths from memory alone.
